@model DAL.Entity.OneToMany.ClassDefination
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

    <div class="content-wrapper">
        <div class="card-header">
            <h2>Silme İşlemi</h2>
        </div>
        <div class="card-body">
            <form asp-action="Delete" asp-controller="ClassDefination" method="post">
                <p>
                    Silmek isteğinizden emin misiniz?
                </p>
                <button class="btn btn-danger">Evet</button>
                <a asp-action="Index" asp-controller="ClassDefination" class="btn">İptal</a>
            </form>
        </div>
    </div>

