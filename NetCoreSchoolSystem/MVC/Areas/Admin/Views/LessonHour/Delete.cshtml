@model DAL.Entity.LessonHour
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

    <div class="content-wrapper">
        <div class="card-header">
            <h2>Ders Saati Sil</h2>
        </div>
        <div class="card-body">
            <form asp-action="Delete" asp-controller="LessonHour" method="post">
                <p>
                    @Model.LesHour isimli saat bilgisini silmek isteğinizden emin misiniz?
                </p>
                <button class="btn btn-danger">Evet</button>
                <a asp-action="Index" asp-controller="LessonHour" class="btn">İptal</a>
            </form>
        </div>
    </div>

