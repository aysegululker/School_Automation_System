@model DAL.Entity.OneToMany.ClassRoom
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

    <div class="content-wrapper">
        <div class="card-header">
            <h2>Sınıf Sil</h2>
        </div>
        <div class="card-body">
            <form asp-action="Delete" asp-controller="ClassRoom" method="post">
                <p>
                    @Model.RoomDepartment isimli branşı silmek isteğinizden emin misiniz?
                </p>
                <button class="btn btn-danger">Evet</button>
                <a asp-action="Index" asp-controller="ClassRoom" class="btn">İptal</a>
            </form>
        </div>
    </div>

