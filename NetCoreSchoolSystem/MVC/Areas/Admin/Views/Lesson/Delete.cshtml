@model DAL.Entity.Lesson
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

    <div class="content-wrapper">
        <div class="card-header">
            <h2>Ders Sil</h2>
        </div>
        <div class="card-body">
            <form asp-action="Delete" asp-controller="Lesson" method="post">
                <p>
                    @Model.LessonName isimli dersi silmek isteğinizden emin misiniz?
                </p>
                <button class="btn btn-danger">Evet</button>
                <a asp-action="Index" asp-controller="Lesson" class="btn">İptal</a>
            </form>
        </div>
    </div>

